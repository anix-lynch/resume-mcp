services:
  - type: web
    name: resume-mcp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server_http.py
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /

